# === uTube Frontend Environment Variables ===
# Copy this file to .env.local and customize for your environment.
# Vite requires all client-side variables to be prefixed with VITE_

# Backend API base URL (used by Axios and urlHelper)
VITE_API_BASE_URL=/api

# Media/storage server base URL (for thumbnails, avatars, video files)
VITE_MEDIA_BASE_URL=http://localhost:8000

# RTMP ingest URL (displayed to streamer for OBS config)
VITE_RTMP_URL=rtmp://127.0.0.1:1935/live

# HTTP-FLV live stream playback base URL
VITE_FLV_BASE_URL=http://127.0.0.1:8080/live
