# === uTube Frontend Environment Variables ===
# Copy this file to .env.local and customize for your environment.
# Vite requires all client-side variables to be prefixed with VITE_

# Backend API base URL (used by Axios and urlHelper)
# In dev: Vite proxy handles /api → http://127.0.0.1:8000
VITE_API_BASE_URL=/api

# Media/storage server base URL (for thumbnails, avatars, video files)
# In dev: leave empty — Vite proxy handles /storage and /uploads → backend
# In prod: set to your server IP/domain
VITE_MEDIA_BASE_URL=

# RTMP ingest URL (displayed to streamer for OBS config)
VITE_RTMP_URL=rtmp://127.0.0.1:1935/live

# HTTP-FLV live stream playback base URL
# In dev: Vite proxy handles /live → http://127.0.0.1:8080
# In prod: set to your server IP/domain + /live
VITE_FLV_BASE_URL=/live

# WebSocket base URL for live chat
# In dev: leave empty — auto-derives from window.location (ws://localhost:3000)
# In prod: set to ws://your-server-ip or wss://your-domain
VITE_WS_URL=
